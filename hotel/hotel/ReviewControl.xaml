<UserControl x:Class="hotel.ReviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:hotel"
             mc:Ignorable="d" Margin="5">
    <Border BorderBrush="{StaticResource Brush.BorderSubtle}" BorderThickness="1" CornerRadius="8" Padding="12" Background="{StaticResource Brush.Surface}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
                <TextBlock Text="Оценка: " FontWeight="Bold" FontSize="25"/>
                <ItemsControl x:Name="StarsPanel" HorizontalAlignment="Left">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="★" Foreground="Gold" FontSize="16"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <TextBlock x:Name="CommentTextBlock" Grid.Row="2" TextWrapping="Wrap" FontStyle="Italic" FontSize="30" MaxWidth="800" HorizontalAlignment="Left"/>

            <TextBlock x:Name="ClientNameBlock" Grid.Row="1" FontStyle="Normal" FontSize="20" Foreground="Gray" Margin="0,6,0,0"/>

            <TextBlock x:Name="DateBlock" Grid.Row="3" Foreground="DarkGray" FontSize="12" Margin="0,4,0,0"/>
        </Grid>
    </Border>
</UserControl>
