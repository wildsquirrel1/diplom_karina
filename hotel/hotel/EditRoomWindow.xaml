<Window x:Class="hotel.EditRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:hotel"
        mc:Ignorable="d"
        Title="Редактирование комнаты" Height="950" MinHeight="730" MaxHeight="1000"  Width="1500" MinWidth="900" MaxWidth="1600" WindowState="Maximized" FontSize="40" WindowStartupLocation="CenterScreen" Icon="images\local-hotel_118960.png">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Grid Grid.Row="0">
            <StackPanel Grid.Row="0" Margin="30,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" MinWidth="260" MaxWidth="640">
                <TextBlock Text="Сотрудник: " FontSize="24" Foreground="Black" Name="emplName" TextWrapping="Wrap"/>
                <TextBlock Text="Роль: " FontSize="24" Foreground="Black" Name="emplRole" TextWrapping="Wrap"/>
            </StackPanel>
            <Image Source="images\icon-icons (2).png" VerticalAlignment="Center" HorizontalAlignment="Center" Width="80" Margin="0 0 640 0"  />
            <Button x:Name="back" Content="✕" Style="{StaticResource CloseWindowButton}" HorizontalAlignment="Right" VerticalAlignment="Top" Click="back_Click" Margin="0,10,20,10"/>
            <TextBlock Text="Редактирование комнаты" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="48"/>
        </Grid>

        <!-- Поля -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!-- Левая колонка -->
            <StackPanel Margin="50,30" VerticalAlignment="Top">
                <Grid Margin="0,0,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Название:"/>
                    <TextBox x:Name="nameTB" Grid.Row="1" Height="60"
                             PreviewTextInput="nameTB_PreviewTextInput"/>
                </Grid>

                <Grid Margin="0,0,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Этаж:"/>
                    <ComboBox x:Name="floorCB" Grid.Row="1" Height="60"/>
                </Grid>

                <Grid Margin="0,0,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Категория:"/>
                    <ComboBox x:Name="categoryCB" Grid.Row="1" Height="60" IsEnabled="False"/>
                </Grid>
            </StackPanel>

            <!-- Правая колонка -->
            <StackPanel Margin="50,30" Grid.Column="1" VerticalAlignment="Top">
                <Grid Margin="0,0,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Цена (не редактируется):"/>
                    <TextBox x:Name="priceTB" Grid.Row="1" Height="60" IsReadOnly="True"/>
                </Grid>

                <Grid Margin="0,0,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Статус:"/>
                    <ComboBox x:Name="statusCB" Grid.Row="1" Height="60"/>
                </Grid>
            </StackPanel>
        </Grid>

        <Button Grid.Row="2" Name="editB" Content="Редактировать" FontSize="40" Padding="20,0" HorizontalAlignment="Center" Margin="0,30" Click="editB_Click"/>
    </Grid>
</Window>
