<UserControl x:Class="hotel.ServiceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:hotel"
             mc:Ignorable="d" 
             MinWidth="280" MaxWidth="460" MinHeight="200" Margin="8">
    <Border x:Name="cardBorder" BorderBrush="{StaticResource Brush.BorderSubtle}" BorderThickness="1" CornerRadius="12" Background="{StaticResource Brush.Surface}" Padding="16">

        <Border.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                To="0.9" Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                To="1" Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="serviceNameText" FontWeight="Bold" FontSize="20" Foreground="#2D2D2D" TextWrapping="Wrap" VerticalAlignment="Center"/>

                <Border Grid.Column="1" x:Name="statusBadge" Padding="8,4" CornerRadius="8" Background="#E8F5E9">
                    <TextBlock x:Name="statusText" Text="Активно" FontSize="12" Foreground="#2E7D32" FontWeight="Medium"/>
                </Border>

            </Grid>

            <TextBlock Grid.Row="1" x:Name="descriptionText" TextWrapping="Wrap" FontSize="14" Foreground="{StaticResource Brush.TextSecondary}" Margin="0,12,0,0" MaxHeight="96"/>

            <Separator Grid.Row="2" Background="#EEEEEE" Margin="0,12,0,12"/>

            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Услуга" FontSize="14" Foreground="#888888"/>
                </StackPanel>

                <Button Grid.Column="1" x:Name="editBtn" Content="изменить" FontSize="14" MinWidth="100" MinHeight="40" Padding="10,6" HorizontalAlignment="Center" Click="editBtn_Click"/>
                <TextBlock Grid.Column="2" x:Name="costText" Text="0 ₽" FontWeight="Bold" FontSize="22" Foreground="{StaticResource Brush.Accent}" HorizontalAlignment="Right"/>
            </Grid>

            <Border x:Name="disabledOverlay" Grid.RowSpan="4" Background="#80FFFFFF" Visibility="Collapsed" CornerRadius="12">
                <TextBlock Text="Недоступно" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" FontWeight="Bold" Foreground="#999999"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
