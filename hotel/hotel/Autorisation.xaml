<Window x:Class="hotel.Autorisation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:hotel"
        mc:Ignorable="d"
        Title="Авторизация" Height="950" MinHeight="730" MaxHeight="1000"  Width="1500" MinWidth="900" MaxWidth="1600" WindowState="Maximized" FontSize="40" WindowStartupLocation="CenterScreen" Icon="images\local-hotel_118960.png">
    <Window.Resources>
        <!-- Стиль для всех кнопок -->
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="34"/>
            <!-- Увеличен шрифт -->
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#8A6DF7"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Width" Value="400"/>
            <!-- Чуть шире -->
            <Setter Property="Height" Value="60"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- Стиль для заголовка -->
        <Style TargetType="TextBlock" x:Key="HotelTitle">
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,40,0,30"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Image Source="images\Hotel.jpg" HorizontalAlignment="Left"/>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <TextBlock Text="Добро пожаловать!" FontSize="70" TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Grid>

            <StackPanel Grid.Row="1" Orientation="Vertical">
                <TextBlock Text="Электронная почта: " Margin="0 0 16 50"/>
                <Border CornerRadius="24" BorderBrush="Black" BorderThickness="1" VerticalAlignment="Center" UseLayoutRounding="True">
                    <TextBox x:Name="emailTB" BorderThickness="0" Background="Transparent" Padding="10 0 0 0"  />
                </Border>
            </StackPanel>

            <Grid Grid.Row="2" >
                <TextBlock Text="Пароль: " VerticalAlignment="Top" Margin="0 0 0 10" />
                <Border CornerRadius="24"  BorderBrush="Black" BorderThickness="1"   Margin="0 30 0 0" VerticalAlignment="Center" UseLayoutRounding="True">
                    <TextBox x:Name="passwordTB" BorderThickness="0" Background="Transparent" Padding="10 0 0 0" Visibility="Hidden"/>
                </Border>

                <Border CornerRadius="24"  BorderBrush="Black" BorderThickness="1" VerticalAlignment="Center" Margin="0 30 0 0" UseLayoutRounding="True">
                    <PasswordBox x:Name="passwordPB" BorderThickness="0" Background="Transparent" Padding="10 0 0 0"  Visibility="Visible"/>
                </Border>
            </Grid>

            <StackPanel Grid.Row="5" Orientation="Vertical" Margin="0 20" >
                <Button Content="Войти" Name="entryB" Click="entryB_Click" Margin="0 0 0 30"  />
                <Button Content="Показать/скрыть пароль" Name="show_hide_pass" Click="show_hide_pass_Click"/>
            </StackPanel>


        </Grid>
    </Grid>
</Window>
