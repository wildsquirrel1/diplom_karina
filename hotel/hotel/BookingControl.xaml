<UserControl x:Class="hotel.BookingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:hotel"
             mc:Ignorable="d" 
             d:DesignHeight="250" MinWidth="300" MaxWidth="440">
    <Border BorderBrush="{StaticResource Brush.BorderSubtle}" BorderThickness="1" CornerRadius="12" Background="{StaticResource Brush.Surface}" Padding="15" Margin="0 10 10 0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок: Номер брони и Статус -->
            <Grid Grid.Row="0">
                <!-- Номер брони -->
                <TextBlock x:Name="tbId" Text="Бронь №..." 
                           FontSize="18" FontWeight="Bold" Foreground="{StaticResource Brush.TextPrimary}"
                           VerticalAlignment="Center" TextWrapping="Wrap" MaxWidth="220"/>

                <!-- Статус -->
                <Border x:Name="statusBadge" HorizontalAlignment="Right" Padding="6,3" CornerRadius="4" VerticalAlignment="Center">
                    <TextBlock x:Name="tbStatus" Text="Статус" FontSize="12" Foreground="White" FontWeight="SemiBold"/>
                </Border>
            </Grid>

            <!-- Основная информация -->
            <StackPanel Grid.Row="1" Margin="0,10,0,10">
                <TextBlock x:Name="tbRoom" Text="Номер: ..." FontSize="16" Margin="0,2" TextWrapping="Wrap" Foreground="{StaticResource Brush.TextPrimary}"/>
                <TextBlock x:Name="tbClient" Text="Клиент: ..." FontSize="14" Foreground="{StaticResource Brush.TextSecondary}" Margin="0,2" TextWrapping="Wrap"/>
                <TextBlock FontSize="14" Foreground="{StaticResource Brush.TextSecondary}" Margin="0,2">
                    <Run Text="Гостей: "/>
                    <Run x:Name="tbGuests" Text="0"/>
                </TextBlock>

                <Separator Margin="0,5"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Заезд:" FontSize="12" Foreground="{StaticResource Brush.TextMuted}"/>
                        <TextBlock x:Name="tbCheckIn" Text=".." FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource Brush.TextPrimary}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Выезд:" FontSize="12" Foreground="{StaticResource Brush.TextMuted}" HorizontalAlignment="Right"/>
                        <TextBlock x:Name="tbCheckOut" Text=".." FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource Brush.TextPrimary}" HorizontalAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <!-- Подвал: Сумма и действия -->
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="tbTotal" Text="Итого: 0 ₽" FontSize="16" FontWeight="Bold" Foreground="{StaticResource Brush.TextPrimary}" VerticalAlignment="Center" TextWrapping="Wrap"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button x:Name="btnCheckIn" Content="Заселить" MinWidth="100" Height="32" FontSize="14" Margin="0,0,8,0" Style="{StaticResource SuccessButton}" Visibility="Collapsed" Click="btnCheckIn_Click"/>
                    <Button x:Name="btnCheckOut" Content="Выселить" MinWidth="100" Height="32" FontSize="14" Margin="0,0,8,0" Style="{StaticResource SuccessButton}" Visibility="Collapsed" Click="btnCheckOut_Click"/>
                    <Button x:Name="btnCancel" Content="Отменить" MinWidth="100" Height="32" FontSize="14" Style="{StaticResource DangerButton}" Visibility="Collapsed" Click="btnCancel_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>  
